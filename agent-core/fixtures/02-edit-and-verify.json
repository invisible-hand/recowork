{
  "name": "edit-and-verify",
  "description": "Edit a file to satisfy a constraint, then run a shell command to verify.",
  "setup": {
    "files": {
      "config.json": "{\n  \"version\": \"0.0.1\",\n  \"debug\": true,\n  \"timeout\": 30\n}\n"
    }
  },
  "goal": "Edit config.json in the current directory so that 'debug' is false and 'timeout' is 60. Then run: cat config.json | grep -E 'debug|timeout' and confirm in your final summary that both values were updated.",
  "verify": {
    "files_exist": ["config.json"],
    "file_contains": {
      "config.json": ["\"debug\": false", "\"timeout\": 60"]
    },
    "min_tool_calls": 2
  }
}
